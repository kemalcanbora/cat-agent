# Clean-trace fixtures for MAST Tier-1 negative tests.
#
# Regenerated by:  python scripts/generate_clean_traces.py
#
# Each clean_*.jsonl is a completed run with a final answer. Patterns covered:
# - clean_math_*     single-tool happy paths
# - clean_multi_*    same tool with different arguments
# - clean_paginate_* pagination / cursor walks (must not trip 1.3)
# - clean_retry_*    transient tool failure then success (must not trip 1.3)
# - clean_reread_*   identical read-only query with other work between
# - clean_chat_*     LLM-only completions
# - clean_mix_*      mixed pagination + distinct queries
#
# Asserted by tests/test_analysis.py::test_clean_fixture_traces_have_no_tier1_findings
