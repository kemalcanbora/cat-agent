# Structured traces demo

```bash
export OLLAMA_API_KEY=your_key_here
export OLLAMA_BASE_URL=https://ollama.com/v1
python examples/trace/run.py
```

Writes `workspace/examples/trace.jsonl` (schema v1.0) and prints `RunTotals`.
